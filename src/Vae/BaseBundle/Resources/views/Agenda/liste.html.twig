
    <h1><a href='{{ path('vae_base_agenda_agenda',{'nomSite' : site.nom} )}}'>Agenda</a></h1>
    <ul id="agenda">
         {% for agenda in agendas %}
            {% if langue == 'fr'%}
                <li>
                    <p>{{agenda.date|date('d/m/y')}}</p>
                    <h3><a class="test" href="{{ path('vae_base_agenda_detail',{'nomSite' : site.nom, 'slugAgenda' : agenda.slug } )}}">{{agenda.titre}}</a></h3>
             {% else %}
                    <p>{{agenda.date|date('m/d/y')}}</p>
                    <h3><a class="test" href="{{ path('vae_base_agenda_detail',{'nomSite' : site.nom, 'slugAgenda' : agenda.slugEn } )}}">{{agenda.titreEn}}</a></h3>
                </li>
             {% endif %}
          {% endfor %}
    </ul>


{#
    Exemple 1 : liste des evenements de l'agenda, ...
    ----------------------------------------------
    DESCRIPTION :       
    liste des 5 prochains evenements encodés dans l'agenda par rapport à la date d'aujourd'hui

    Nom du Bundle: VaeBaseBundle
    Controller: Agenda
    Action    : liste
    ----------------------------------------------

    A INTEGRER : 
        - dans le template generale
        - {{ render(controller('VaeBaseBundle:Agenda:liste', {'nomSite' : site.nom } ))}}

    VARIABLES DISPONIBLES:
        - agenda: ARRAY/OBJ : 
                     propriétés : id, titre, titreEn, contenu, contenuEn, date slug, slugEn
        
        - langue
        - site.nom
    ----------------------------------------------                
    POUR TESTER : app_dev.php/$_locale/nom du site/accueil
    ----------------------------------------------
#}

